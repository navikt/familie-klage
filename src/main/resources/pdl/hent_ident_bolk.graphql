query($identer: [ID!]!, $gruppe: [IdentGruppe!]) {
    hentIdenterBolk(identer: $identer, grupper: $gruppe) {
        code
        ident
        identer {
            ident
            historisk
        }
    }
}
